# Verificación del material
Se ejecutaron en orden las 28 celdas de código en un mismo espacio de Python, con visualización tabular omitida y Matplotlib sin interfaz gráfica. Se ejecutaron también las ocho soluciones y se comprobaron doce casos de datos inválidos, producción cero y totales de referencia.

No se realizó una sesión real de Jupyter/Colab ni una prueba visual de ipywidgets, que no estaba instalado. La alternativa sin widgets se ejecutó correctamente. El notebook se entrega sin salidas guardadas para que el estudiante lo ejecute.

Entorno de comprobación: Python 3.13.7, NumPy 2.5.3, pandas 3.0.5, Matplotlib 3.11.2. Las dependencias no se fijan a estas versiones; son el registro del entorno comprobado.
